kind = "web"
previewPath = "/"
title = "Digitrade 2.0 Landing Page"
version = "1.0.0"
id = "artifacts/digitrade"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19185

[services.development]
run = "pnpm --filter @workspace/digitrade run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/digitrade", "run", "build" ]
publicDir = "artifacts/digitrade/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19185"
BASE_PATH = "/"
